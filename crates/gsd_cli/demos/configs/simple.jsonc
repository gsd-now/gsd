// Example invocation:
// pnpm gsd run --pool <POOL> --initial '[{"kind": "Start", "value": {}}]' simple.jsonc
{
  "$schema": "../schemas/config.schema.json",
  "steps": [
    {
      "name": "Start",
      "action": {
        "kind": "Pool",
        "instructions": "This is the starting step. Return an empty array to finish."
      },
      "next": []
    }
  ]
}
